# front-end

React 19 + Vite 6 + Tailwind CSS 4 client for the Smart Ingredient Analyzer.

```bash
npm ci
cp .env.example .env   # VITE_API_URL=http://localhost:5000
npm run dev
```

Setup, architecture and limitations are documented in the
[repository README](../README.md).
